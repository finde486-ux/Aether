[project]
name = "project-aether"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2.0",
    "graphiti-sdk>=1.0.0",
    "mcp-sdk>=0.5.0",
    "pydantic>=2.10.0",
    "rich>=13.9.0",
    "psutil"
]
